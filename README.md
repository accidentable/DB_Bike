# 🚲 DB_Bike - 서울 따릉이 통합 관리 시스템

광운대학교 따릉이 서비스를 위한 풀스택 웹 애플리케이션입니다. 사용자는 자전거를 대여하고 포인트를 적립하며, 관리자는 시스템 전체를 모니터링하고 관리할 수 있습니다.

---

## 📋 목차

- [프로젝트 개요](#-프로젝트-개요)
- [주요 기능](#-주요-기능)
- [기술 스택](#-기술-스택)
- [프로젝트 구조](#-프로젝트-구조)
- [개발 환경 설정](#-개발-환경-설정)
- [실행 방법](#-실행-방법)
- [API 문서](#-api-문서)
- [데이터베이스](#-데이터베이스)
- [배포](#-배포)

---

## 📌 프로젝트 개요

**DB_Bike**는 자전거 공유 서비스의 전체 생명주기를 관리하는 통합 플랫폼입니다.

- **대상**: 광운대학교 학생 및 교직원, 시스템 관리자
- **목적**: 자전거 대여 관리, 사용자 포인트 시스템, 커뮤니티 기능 제공
- **방식**: 웹 기반 반응형 애플리케이션

---

## ✨ 주요 기능

### 👤 사용자 기능

#### 1. **회원 관리**
- 일반 회원가입/로그인
- 카카오 로그인 (OAuth)
- 비밀번호 찾기
- 프로필 관리

#### 2. **자전거 대여**
- 대여소별 자전거 현황 조회
- 실시간 대여소 위치 지도 표시 (Kakao Maps)
- 자전거 대여/반납
- 대여 기록 조회

#### 3. **포인트 시스템**
- 대여를 통한 포인트 적립
- 포인트 기반 이용권 구매
- 포인트 거래 내역 관리
- 이용권 종류:
  - 1시간권 (1,000원)
  - 1일권 (2,000원)
  - 정기권 (5,000원)
  - 연간권 (30,000원)

#### 4. **업적 및 랭킹**
- 8가지 업적 달성 시스템:
  - 첫 걸음 (첫 이용 완료)
  - 초보/중급 라이더 (10회, 50회)
  - 출퇴근 마스터 (10일 연속)
  - 장거리 라이더 (누적 거리 100km)
  - 환경 지킴이 (누적 500km)
  - 전국구 (50개 이상 대여소)
  - 단골 회원 (100회)
- 주간 랭킹 시스템 (거리/이용횟수 기준)

#### 5. **커뮤니티**
- 게시글 작성/수정/삭제
- 댓글 및 좋아요 기능
- 파일 첨부 (이미지/문서)
- 게시글 조회수 및 고정 기능

#### 6. **즐겨찾기**
- 대여소 즐겨찾기 관리

### 🔐 관리자 기능

#### 1. **대시보드**
- 전체 사용자 수
- 자전거/대여소 현황
- 누적 대여 거리
- 실시간 활동 로그

#### 2. **데이터 시각화**
- 지역구별 대여소 현황 (파이차트)
- 대여소별 대여율 (가로 막대그래프)
- 통계 카드로 주요 수치 표시

#### 3. **사용자 관리**
- 사용자 조회 및 수정
- 사용자 삭제
- 포인트 거래 내역 조회
- 관리자가 사용자에게 이용권 부여

#### 4. **자전거 관리**
- 자전거 목록 조회
- 새 자전거 추가
- 자전거 정보 수정
- 자전거 삭제

#### 5. **대여소 관리**
- 대여소 정보 조회/수정/삭제
- 새 대여소 추가
- 대여소 상태 관리

#### 6. **분석**
- Activity Log 조회
- 지역구별 통계 분석
- 대여소별 대여율 분석

---

## 🛠 기술 스택

### **Backend**
- **Runtime**: Node.js
- **Framework**: Express.js 5.x
- **Database**: PostgreSQL
- **Authentication**: JWT (JSON Web Tokens)
- **Password Hashing**: bcrypt
- **File Upload**: Multer
- **Scheduling**: node-schedule (주간 랭킹 업데이트)
- **Email**: Nodemailer (인증 코드 발송)

### **Frontend**
- **Framework**: React 19.x
- **Language**: TypeScript
- **Build Tool**: Vite
- **Styling**: Tailwind CSS
- **UI Component**: shadcn/ui (Radix UI 기반)
- **Routing**: React Router v7
- **HTTP Client**: Axios
- **Charts**: Recharts (데이터 시각화)
- **Maps**: Kakao Maps SDK
- **Icons**: Lucide React

### **DevOps & Tools**
- **Containerization**: Docker & Docker Compose
- **Package Manager**: npm
- **Linting**: ESLint
- **Version Control**: Git

---

## 📁 프로젝트 구조

```
DB_Bike/
├── backend/                          # Node.js 백엔드 서버
│   ├── db/                           # 데이터베이스 관련
│   │   ├── init.sql                  # DB 초기화 스크립트
│   │   ├── add_achievements_*.sql    # 업적 추가 스크립트
│   │   └── migrate_kakao_id.js       # 카카오 ID 마이그레이션
│   ├── src/
│   │   ├── api/                      # API 라우터
│   │   │   ├── admin.routes.js       # 관리자 API
│   │   │   ├── auth.routes.js        # 인증 API
│   │   │   ├── rental.routes.js      # 대여 API
│   │   │   ├── post.routes.js        # 커뮤니티 API
│   │   │   ├── ranking.routes.js     # 랭킹 API
│   │   │   ├── achievement.routes.js # 업적 API
│   │   │   └── ...                   # 기타 API
│   │   ├── repositories/             # 데이터 접근 계층
│   │   │   ├── member.repository.js
│   │   │   ├── rental.repository.js
│   │   │   ├── stats.repository.js
│   │   │   └── ...
│   │   ├── services/                 # 비즈니스 로직 계층
│   │   │   ├── admin.service.js      # 관리자 로직
│   │   │   ├── auth.service.js       # 인증 로직
│   │   │   ├── rental.service.js     # 대여 로직
│   │   │   ├── scheduler.service.js  # 스케줄링 (데이터 동기화)
│   │   │   ├── weekly-ranking-scheduler.js  # 주간 랭킹
│   │   │   └── ...
│   │   ├── middleware/               # Express 미들웨어
│   │   │   ├── auth.middleware.js    # JWT 검증
│   │   │   └── admin.middleware.js   # 관리자 권한 확인
│   │   └── config/
│   │       └── db.config.js          # DB 연결 설정
│   ├── app.js                        # Express 앱 초기화
│   ├── server.js                     # 서버 메인 파일
│   ├── seedDatabase.js               # DB 시딩 스크립트
│   ├── docker-compose.yml            # Docker 설정
│   ├── package.json
│   └── README.md
│
├── frontend/                         # React 프론트엔드
│   ├── src/
│   │   ├── api/                      # API 클라이언트
│   │   │   ├── client.ts             # Axios 인스턴스
│   │   │   ├── adminApi.ts           # 관리자 API
│   │   │   ├── authApi.ts            # 인증 API
│   │   │   ├── rentalApi.ts          # 대여 API
│   │   │   ├── postApi.ts            # 커뮤니티 API
│   │   │   └── ...
│   │   ├── components/               # React 컴포넌트
│   │   │   ├── layout/               # 레이아웃
│   │   │   │   ├── Header.tsx        # 네비게이션 헤더
│   │   │   │   └── Footer.tsx
│   │   │   ├── ui/                   # shadcn/ui 컴포넌트
│   │   │   │   ├── card.tsx
│   │   │   │   ├── button.tsx
│   │   │   │   ├── chart.tsx         # 차트 기본 컴포넌트
│   │   │   │   └── ...
│   │   │   ├── StationMap.tsx        # Kakao Maps 지도
│   │   │   ├── HowToUse.tsx          # 사용 안내
│   │   │   └── ...
│   │   ├── pages/                    # 페이지 컴포넌트
│   │   │   ├── AdminDashboardpage.tsx # 관리자 대시보드
│   │   │   ├── RankingPage.tsx       # 랭킹 페이지
│   │   │   ├── LoginPage.tsx         # 로그인
│   │   │   ├── SignupPage.tsx        # 회원가입
│   │   │   ├── PostPage.tsx          # 커뮤니티
│   │   │   └── ...
│   │   ├── contexts/                 # React Context
│   │   ├── utils/                    # 유틸리티
│   │   │   └── i18n.ts               # 다국어 지원
│   │   ├── styles/                   # CSS 파일
│   │   ├── App.tsx                   # 메인 앱
│   │   ├── main.tsx                  # 엔트리포인트
│   │   └── vite-env.d.ts
│   ├── public/                       # 정적 자산
│   ├── package.json
│   ├── vite.config.ts
│   ├── tsconfig.json
│   └── README.md
│
└── README.md                         # 이 파일
```

---

## 🚀 개발 환경 설정

### 필수 요구사항

- **Node.js**: v18 이상
- **Docker Desktop**: 최신 버전
- **Git**: 최신 버전
- **환경변수**: `.env` 파일 설정

### 1단계: 저장소 클론

```bash
git clone [Repository URL]
cd DB_Bike
```

### 2단계: Backend 설정

```bash
cd backend

# 1. 종속성 설치
npm install

# 2. .env 파일 생성 및 설정
# .env 파일에 다음을 추가하세요:
# DATABASE_URL=postgresql://user:password@localhost:5432/ddareungi
# JWT_SECRET_KEY=your-secret-key
# ADMIN_PASSWORD=admin123
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password

# 3. Docker 시작 (PostgreSQL)
docker-compose up -d

# 4. DB 초기화 및 시딩
npm run db:seed

# 5. 서버 시작
npm run dev
```

### 3단계: Frontend 설정

```bash
cd frontend

# 1. 종속성 설치
npm install

# 2. 개발 서버 시작
npm run dev

# 브라우저에서 http://localhost:5173 접속
```

---

## 🏃 실행 방법

### Backend 실행

```bash
cd backend

# 개발 모드 (자동 재시작)
npm run dev

# 운영 모드
npm start

# DB 시딩 (초기 데이터 삽입)
npm run db:seed

# 카카오 ID 마이그레이션
npm run db:migrate-kakao
```

**백엔드 서버**: `http://localhost:3000`

### Frontend 실행

```bash
cd frontend

# 개발 서버
npm run dev

# 빌드
npm run build

# 빌드 결과 미리보기
npm run preview

# Linting
npm run lint
```

**프론트엔드**: `http://localhost:5173`

### 동시 실행 (권장)

터미널을 2개 열어서:

**터미널 1 (Backend)**:
```bash
cd backend && npm run dev
```

**터미널 2 (Frontend)**:
```bash
cd frontend && npm run dev
```

---

## 📡 API 문서

### Authentication API
- `POST /api/auth/signup` - 회원가입
- `POST /api/auth/login` - 로그인
- `POST /api/auth/kakao` - 카카오 로그인
- `POST /api/auth/verify-email` - 이메일 인증

### User API
- `GET /api/users/profile` - 프로필 조회
- `PUT /api/users/profile` - 프로필 수정

### Rental API
- `GET /api/rentals` - 대여 기록 조회
- `POST /api/rentals/start` - 대여 시작
- `POST /api/rentals/end` - 대여 종료

### Station API
- `GET /api/stations` - 대여소 목록
- `GET /api/stations/:id` - 대여소 상세 정보
- `POST /api/stations/favorites` - 즐겨찾기 추가

### Post API (커뮤니티)
- `GET /api/posts` - 게시글 목록
- `POST /api/posts` - 게시글 작성
- `PUT /api/posts/:id` - 게시글 수정
- `DELETE /api/posts/:id` - 게시글 삭제
- `POST /api/posts/:id/comments` - 댓글 작성
- `POST /api/posts/:id/like` - 좋아요

### Ranking API
- `GET /api/rankings/weekly` - 주간 랭킹
- `GET /api/rankings/distance` - 거리 랭킹

### Achievement API
- `GET /api/achievements` - 업적 조회
- `GET /api/achievements/member/:id` - 사용자 업적

### Admin API
- `POST /api/admin/auth-password` - 관리자 인증
- `GET /api/admin/stats` - 대시보드 통계
- `GET /api/admin/users` - 사용자 목록
- `GET /api/admin/district-stats` - 지역구별 통계
- `GET /api/admin/station-rental-rates` - 대여소별 대여율
- `GET /api/admin/activity-logs` - 활동 로그

자세한 내용은 [API 문서](backend/docs/따릉이API.md)를 참고하세요.

---

## 🗄 데이터베이스

### 주요 테이블

- **members**: 사용자 정보
- **bikes**: 자전거
- **stations**: 대여소
- **rentals**: 대여 기록
- **posts**: 커뮤니티 게시글
- **comments**: 댓글
- **achievements**: 업적 정보
- **member_achievements**: 사용자별 달성 업적
- **weekly_rankings**: 주간 랭킹
- **point_transactions**: 포인트 거래
- **ticket_types**: 이용권 종류
- **member_tickets**: 사용자 이용권

### DB 초기화

```bash
# Docker PostgreSQL 실행 확인
docker-compose ps

# DB 시딩 (전체 초기화)
npm run db:seed
```

### Docker 포트

- **PostgreSQL**: `localhost:5433` (외부)
- **컨테이너 내부**: `localhost:5432`

---

## 📊 데이터 시각화

### 시각화 컴포넌트

1. **통계 카드**: 핵심 KPI 표시
2. **파이차트**: 지역구별 대여소 분포
3. **막대그래프**: 대여소별 대여율
4. **지도**: Kakao Maps 기반 대여소 위치

### 차트 라이브러리

- **Recharts**: 반응형 차트 컴포넌트
- **shadcn/ui Chart**: 커스텀 차트 래퍼

---

## 🎨 주요 페이지

### 사용자 페이지

- **홈**: 주요 기능 안내 및 지도
- **로그인/회원가입**: 일반 로그인 및 카카오 로그인
- **대여소 지도**: Kakao Maps 기반 실시간 위치
- **랭킹**: 주간 TOP 3 및 전체 랭킹
- **커뮤니티**: 게시글 작성/조회/댓글
- **마이페이지**: 프로필, 대여 기록, 포인트, 업적

### 관리자 페이지

- **대시보드**: 통계 및 시각화
  - 전체 사용자/자전거/대여소 수
  - 지역구별 대여소 현황 (파이차트)
  - 대여소별 대여율 (막대그래프)
  - 실시간 활동 로그

- **사용자 관리**: 사용자 조회, 수정, 삭제, 이용권 부여
- **자전거 관리**: 자전거 조회, 추가, 수정, 삭제
- **대여소 관리**: 대여소 조회, 추가, 수정, 삭제

---

## 🔑 환경변수 설정

### Backend (.env)

```env
# 데이터베이스
DATABASE_URL=postgresql://user:password@localhost:5433/ddareungi

# JWT
JWT_SECRET_KEY=your-secret-key-here

# 관리자
ADMIN_PASSWORD=admin123

# 이메일 (Gmail)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# 카카오 (선택사항)
KAKAO_CLIENT_ID=your-kakao-id
KAKAO_REDIRECT_URI=http://localhost:3000/api/auth/kakao

# 포트
PORT=3000
```

### Frontend (.env)

```env
VITE_API_URL=http://localhost:3000
```

---

## 📈 주요 기능 상세

### 포인트 시스템

- 1km 대여 시 10포인트 적립
- 포인트로 이용권 구매 가능
- 포인트 거래 내역 관리

### 업적 시스템

8가지 업적 조건:
- 첫 걸음: 첫 대여 완료
- 초보 라이더: 10회 이용
- 중급 라이더: 50회 이용
- 출퇴근 마스터: 10일 연속
- 장거리 라이더: 누적 100km
- 환경 지킴이: 누적 500km
- 전국구: 50개 대여소
- 단골 회원: 100회 이용

### 주간 랭킹

매주 월요일 00:00에 자동 계산:
- 전주 사용자의 거리 기반 순위 결정
- TOP 3에게 추가 포인트 지급
- 히스토리 저장

---

## 🐳 Docker 관리

### PostgreSQL 시작

```bash
cd backend
docker-compose up -d
```

### 상태 확인

```bash
docker-compose ps
```

### 로그 확인

```bash
docker-compose logs -f
```

### 중지

```bash
docker-compose down
```

### 포트 충돌 해결

```bash
# 5433 포트 사용 중인 프로세스 확인
netstat -ano | findstr :5433

# PID로 프로세스 종료
taskkill /PID [PID] /F
```

---

## 🚀 배포

### Backend 배포 (예: Vercel, Railway, Render)

1. 환경변수 설정
2. 빌드: `npm run build`
3. 실행: `npm start`

### Frontend 배포 (예: Vercel, Netlify)

```bash
npm run build
# dist/ 폴더를 배포
```

---

## 🐛 트러블슈팅

### DB 연결 실패

```bash
# Docker 실행 확인
docker-compose ps

# DB 재시작
docker-compose down
docker-compose up -d

# 포트 확인
netstat -ano | findstr :5433
```

### API 호출 실패

- CORS 설정 확인 (`backend/app.js`)
- JWT 토큰 유효성 확인
- 백엔드 서버 실행 확인

### 지도 표시 안 됨

- Kakao API 키 확인
- 인터넷 연결 확인

### 이메일 발송 안 됨

- Gmail App Password 설정 확인
- 환경변수 설정 확인
- 스팸 폴더 확인

---

## 📝 License

ISC

---

## 👥 개발팀

- **Backend**: Node.js/Express
- **Frontend**: React/TypeScript
- **Database**: PostgreSQL
- **DevOps**: Docker

---

## 📞 연락처

문제 발생 시 GitHub Issues를 통해 보고해주세요.

---

## 🔄 최근 업데이트

- ✅ 주석 정리 (LLM 스타일 제거)
- ✅ 데이터 시각화 구현 (Recharts)
- ✅ 카카오 로그인 통합
- ✅ 포인트/이용권 시스템
- ✅ 주간 랭킹 자동화
- ✅ 커뮤니티 기능

---

**Happy Cycling! 🚴**
